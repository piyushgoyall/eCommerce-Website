<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | ClassyKart</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <div class="container mx-auto p-6">
      <h1 class="text-4xl font-bold text-center text-blue-600 mb-6">
        Welcome to ClassyKart
      </h1>

      <% if (products && products.length > 0) { %>
      <div
        class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6"
      >
        <% products.forEach(product => { %>
        <div class="bg-white shadow-md rounded-md p-4">
          <img
            src="<%= product.image %>"
            alt="<%= product.name %>"
            class="w-full h-48 object-cover mb-4 rounded-md"
          />
          <h3 class="text-lg font-semibold text-gray-800 mb-2">
            <%= product.name %>
          </h3>
          <p class="text-gray-600 mb-4">
            <%= product.description || "No description available" %>
          </p>
          <p class="text-xl font-bold text-blue-600"><%= product.price %></p>
          <button
            class="w-full bg-blue-600 text-white py-2 mt-4 rounded-md hover:bg-blue-700"
          >
            Buy Now
          </button>
        </div>
        <% }) %>
      </div>
      <% } else { %>
      <p class="text-center text-gray-600">
        No products available at the moment.
      </p>
      <% } %>
    </div>
  </body>
</html>
