<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ClassyCart - Signup</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-r from-gray-900 via-gray-800 to-teal-900 min-h-screen flex justify-center items-center"
  >
    <div class="bg-gray-700 rounded-2xl shadow-2xl p-10 max-w-lg w-full">
      <h1 class="text-4xl font-extrabold text-gray-100 mb-6 text-center">
        Join <span class="text-teal-400">ClassyCart</span>
      </h1>
      <p class="text-gray-300 text-center mb-8">
        Shop smarter, faster, and in style. Sign up now!
      </p>

      <form action="/users/create" method="POST" class="space-y-6">
        <div>
          <label
            for="fullname"
            class="block text-sm font-semibold text-gray-300 mb-2"
          >
            Full Name
          </label>
          <input
            type="text"
            id="fullname"
            name="fullname"
            class="block w-full px-4 py-3 rounded-lg border border-gray-600 bg-gray-800 text-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-teal-500"
            placeholder="John Doe"
            required
          />
        </div>
        <div>
          <label
            for="email"
            class="block text-sm font-semibold text-gray-300 mb-2"
          >
            Email
          </label>
          <input
            type="email"
            id="email"
            name="email"
            class="block w-full px-4 py-3 rounded-lg border border-gray-600 bg-gray-800 text-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-teal-500"
            placeholder="john@example.com"
            required
          />
        </div>
        <div>
          <label
            for="password"
            class="block text-sm font-semibold text-gray-300 mb-2"
          >
            Password
          </label>
          <input
            type="password"
            id="password"
            name="password"
            class="block w-full px-4 py-3 rounded-lg border border-gray-600 bg-gray-800 text-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-teal-500"
            placeholder="********"
            required
          />
        </div>
        <div>
          <label
            for="contact"
            class="block text-sm font-semibold text-gray-300 mb-2"
          >
            Contact Number
          </label>
          <input
            type="number"
            id="contact"
            name="contact"
            class="block w-full px-4 py-3 rounded-lg border border-gray-600 bg-gray-800 text-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-teal-500"
            placeholder="1234567890"
          />
        </div>

        <button
          type="submit"
          class="w-full py-3 px-6 bg-gradient-to-r from-teal-500 to-teal-600 text-white rounded-lg shadow-md text-lg font-bold hover:from-teal-400 hover:to-teal-500 focus:outline-none focus:ring-2 focus:ring-teal-400 focus:ring-offset-2"
        >
          Create Account
        </button>
      </form>

      <p class="text-center text-sm text-gray-400 mt-6">
        Already have an account?
        <a
          href="/users/login"
          class="text-teal-400 font-semibold hover:underline"
        >
          Login here </a
        >.
      </p>
    </div>
  </body>
</html>
